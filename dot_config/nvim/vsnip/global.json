{
  "uuid": {
    "prefix": "uuid",
    "body": [
      "${VIM:system('python -c \"import uuid, sys;sys.stdout.write(str(uuid.uuid4()))\"')}"
    ]
  },
  "date": {
    "prefix": "date",
    "body": [
      "${VIM:system('deno eval \"console.log(new Date().toISOString())\"')}"
    ]
  }
}
